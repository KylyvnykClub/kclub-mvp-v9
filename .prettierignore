# Dependencies and build output (when the app exists).
node_modules/
.next/
.next-stale-*/
dist/
coverage/

# Lockfiles are generated, not formatted.
pnpm-lock.yaml
package-lock.json
yarn.lock

# Local-only session state (never committed).
.state/

# Claude Code configuration (auto-generated).
.claude/

# Claude Design canvas mockups - design references, not application code.
src/admin/
