# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
.next-stale-*/
# pnpm verify builds here so it cannot clobber a running dev server
.next-verify/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Test output
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# Secrets and environment. Only .env.example is committed, and it is generated
# from src/env.ts — see docs/technology.md and CONTRIBUTING.md.
.env
.env.local
.env.*.local
CREDENTIALS.md

# Wrong package manager lockfile (project uses pnpm)
package-lock.json

# Infrastructure. The lock file IS committed; state and variable files are not.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# Vercel
.vercel/

# Tooling artefacts
__pycache__/
*.py[cod]
*.log
npm-debug.log*
pnpm-debug.log*

# Operating system and editors. .vscode/ is committed deliberately — shared
# editor settings are part of the 30-minute setup target.
.DS_Store
Thumbs.db
.idea/
*.swp
NUL
outputs/

# Sentry Config File
.env.sentry-build-plugin


.codex
# design-sync machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
